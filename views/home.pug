extends layouts/main.pug
include mixins/videoBlock.pug

block content
    .video 
        each item in videos
            +videoBlock({
                id: item.id,
                title: item.title,
                views: item.views,
                videoFile: item.fileUrl
            })